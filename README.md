#Мой проект это собственный структура данных строка я реализовал большинство функций для работы с ней.
#Вот структура.
...C
typedef struct str
{
    uint8_t* data;
    size_t capasity;
    size_t size;
}str_t;
...
Для компиляции используется Cmake нужно создать папку build и выполнить команду cmake .. и make скомпилуются 2 файлы my_string.o и my_string_test.o с библиоткой все понятно а второй файл это тесты запустите их чтоб проверить работу.
